## 🗑️ 배포 제거 시 정리 작업

### ec2 runner 제거

> Settings → Actions → Runners → ec2 → Remove runner

(필요 시) ec2에서 `~/actions-runner/_work`, docker image 정리

### 환경 제거

> Settings → Environments → api-production → Delete
